# LV2 feature fixtures

This directory contains lightweight fixture data for validating the LV2 feature plumbing in the loader.

- `ratatouille-feature-snapshot.json` captures the relevant metadata from the Ratatouille bundle for feature-focused tests.
- `atom-path-fixture.json` captures the atom:Path and state/worker-related expectations used by the loader.
